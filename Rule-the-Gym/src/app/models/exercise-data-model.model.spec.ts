import { ExerciseDataModel } from './exercise-data-model.model';

describe('ExerciseDataModel', () => {
  it('should create an instance', () => {
    expect(new ExerciseDataModel()).toBeTruthy();
  });
});
